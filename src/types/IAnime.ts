export interface IAnime {
    id: number;    
    title: string;
    poster: string;
    originTitle: string;
    description: string;
    geners: string[];
    images: string[];
    director: string;
    releaseDate: string;
    author: number;
}