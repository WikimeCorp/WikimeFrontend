import { FC } from "react";
import "../styles/Loading.css";

const Loading: FC = () => {
    
    return (
        <div className="loading-page">
            <div>
                <h1>Loading...</h1>
            </div>
        </div>
    );
};

export default Loading;